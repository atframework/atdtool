---
applyTo: "README.md,docs/**/*.md,Plan.md"
---

- Document the repository's real behavior, not assumptions copied from outside sample projects.
- Keep examples generic, stable, and consistent with current commands and tests.
- When code or CLI behavior changes, keep `README.md` and the relevant `docs/` pages in sync.
